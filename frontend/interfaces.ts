export interface City {
    id: number;
    name: string;
    q: string;
}

export interface ImportRecord {
    use_date: string;
    name: string;
    regist_date: string;
    import_judge_date: string;
    import_date: string;
}
