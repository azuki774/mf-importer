version: '3.8'

services:
  db:
    image: mariadb:10
    restart: always
    environment:
      MYSQL_DATABASE: mfimporter
      MYSQL_ROOT_PASSWORD: password
      TZ: Asia/Tokyo
    volumes:
      - db-data:/var/lib/mysql
    ports:
      - "3306:3306"

volumes:
  db-data:
